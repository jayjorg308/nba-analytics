# Assisted makes is a distinct subsection of the second act

v2.5 adds **ASSISTED MAKES** after the existing Case 2 chart and table inside the Shot Creation second act. The two-axis headline and primary court/zone evidence remain unchanged, and assist analysis does not become a third headline card or third act; the page continues from its primary answer to aggregate creation context and then to per-shot Case 3 evidence.

The new evidence does not share the existing creation-value chart or table because its hero-only assisted-share denominator and absent league baseline are semantically different from Case 2 PPS-vs-league contexts. Made-shot tooltips gain assist status, while estimated shot-clock remaining appears there only if its independent audit gate passes.

**Amendment (2026-07-20) — the visual rank graduated; the argument rank did not.** ADR-0051 gave every act the same full-width opener, and the act kickers number ASSISTED MAKES `03 · THE CREDIT` — a full third beat in the page's reading order, named for the scorer's ledger (this ADR's own reason the section could not share the creation surfaces: a different measurement, not another arrival circumstance). What this ADR protects is unchanged: assist analysis is still not a third headline card, not a third axis of the ADR-0016 decomposition, and verdicts still do not cite assist data (ADR-0049's discipline) until a hero ships declared assist-share claims. The numbering names reading order, not a new decomposition; the section still renders inside the second act's DOM.
